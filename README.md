# Bayesian Network for Predicting Goal Probability (xG)

This project builds an expected goals (xG) model using StatsBomb Open Data. The target is to estimate the probability that a football shot becomes a goal:

```math
P(Goal = Yes \mid shot\ information)
